# dshw-p01
## 1.1 自选股票清单

| 股票代码 | 股票名称 | 行业 | 选股理由 |
|---|---|---|---|
| 600036 | 招商银行 | 银行 | 零售银行代表性强，市场关注度高，适合作为银行板块样本。 |
| 601398 | 工商银行 | 银行 | 大型国有银行代表，规模大、交易活跃，便于进行横向比较。 |
| 000625 | 长安汽车 | 汽车 | 自主品牌车企代表，受行业景气与新能源转型影响明显。 |
| 600104 | 上汽集团 | 汽车 | 传统整车龙头，适合与长安汽车做行业内比较。 |
| 600519 | 贵州茅台 | 白酒 | 高端白酒龙头，估值和业绩都具有代表性。 |
| 000858 | 五粮液 | 白酒 | 白酒核心龙头之一，可与茅台构成行业对照样本。 |
| 600028 | 中国石化 | 能源 | 传统能源央企代表，受国际油价和宏观周期影响较强。 |
| 601857 | 中国石油 | 能源 | 能源板块核心公司，行业代表性强，便于研究板块联动。 |
| 600050 | 中国联通 | 通讯 | 基础电信运营商代表，适合观察稳健型行业特征。 |
| 000063 | 中兴通讯 | 通讯 | 通信设备龙头之一，兼具成长性与行业波动特征。 |

## 数据来源说明

本部分股票日度行情数据使用 `baostock` 获取，调用接口为 `bs.query_history_k_data_plus()`，复权方式为后复权（`adjustflag='1'`）。

## 1.2 市场指数
| 指数代码 | 指数名称 | 选择理由 |
|---|---|---|
| 000300 | 沪深300 | 作为后续 CAPM 分析的市场基准。 |
| 000001 | 上证综指 | 反映沪市整体走势，可作为综合市场指数补充。 |

- 数据时间范围：与股票数据一致，为 2020-01-01 至今
- 保存路径：`data/index/`
- 数据来源：`baostock`

## 1.3 宏观经济指标
| 指标名称 | 选择理由 |
|---|---|
| CPI同比增速 | CPI 反映通胀压力，会影响实际利率、估值折现率和居民购买力，从而影响股票市场表现。 |
| 1年期LPR | LPR 会影响企业融资成本和估值贴现率，因此与股票市场定价具有直接关系。 |

- 保存路径：`data/macro/`
- 数据来源：`akshare`

## 1.4 财务指标
本部分获取 10 只股票最近 5 个完整年度（2020–2024）的两类财务指标：

- `ROE`
- `资产负债率`

并整理成长格式 `finance_long.csv`，字段为：

- `code`
- `year`
- `indicator`
- `value`

保存路径：`data/finance/`

## 1.5 下载日志
所有下载任务都会自动写入 `download_log.txt`，格式示例：

```text
[2026-04-05 10:23:45] SUCCESS  index_000300       shape=(1200, 8)
[2026-04-05 10:23:48] SUCCESS  macro_cpi_yoy      shape=(60, 3)
[2026-04-05 10:23:51] FAILED   finance_600036     Error: No data returned
```

<!-- part2_start -->
## 第二部分：数据存储与管理

### 2.1 目录结构

本项目按照作业要求，使用 Python 代码自动创建并维护如下目录结构：

```text
dshw-p01/
├── README.md
├── report.html
├── requirements.txt
├── .gitignore
├── 01_download.ipynb
├── 02_clean.ipynb
├── 03_analysis.ipynb
├── data/
│   ├── stock/
│   ├── index/
│   ├── macro/
│   ├── finance/
│   ├── clean/
│   │   ├── stock_clean.csv
│   │   └── stock_clean.parquet
│   └── combined/
│       └── combined_data.csv
├── output/
└── download_log.txt
```

其中，所有文件名均使用小写字母和下划线命名，不包含空格与中文。

### 2.2 数据存储格式

#### 方式 A：CSV（必做）

- 所有原始数据继续以 CSV 格式保存；
- 清洗后的股票面板数据保存为 `data/clean/stock_clean.csv`；
- 合并后的综合数据保存为 `data/combined/combined_data.csv`。

CSV 的优点在于：

1. 通用性强，几乎所有数据分析软件都可以直接读取；
2. 可读性高，便于人工检查和调试；
3. 适合课程作业中的小中规模数据交换与提交。

CSV 的局限在于：

1. 不保存严格的数据类型信息，读取时往往需要再次转换类型；
2. 文件体积通常较大；
3. 不支持高效的按列读取，在列很多或数据量较大时性能较弱。

#### 方式 B：Parquet（进阶）

本作业选择 **方式 B：Parquet** 作为进阶格式，并将清洗后的股票数据额外保存为：

- `data/clean/stock_clean.parquet`

选择 Parquet 的理由是：

1. Parquet 是列式存储格式，适合金融面板数据中“按列读取”的分析场景；
2. 它能够保留更明确的 schema（类型契约），减少重复清洗成本；
3. 与 CSV 相比，Parquet 往往在读取效率和压缩体积方面更有优势，便于展示对进阶存储工具的掌握。

### 2.3 Parquet 演示结果

本次在 `02_clean.ipynb` 中完成了以下演示：

- 只读取 Parquet 中的 `date`、`code`、`close` 三列；
- 使用 `pyarrow.parquet.read_schema()` 查看 schema；
- 比较 CSV 与 Parquet 的读取耗时和文件体积。

本次实测结果如下：

- CSV 读取耗时：`0.0371` 秒
- Parquet 读取耗时：`0.0071` 秒
- CSV 文件大小：`1835.8` KB
- Parquet 文件大小：`857.8` KB

文字说明：

- 在本次数据规模下，CSV 与 Parquet 的读取速度差异不算特别大。
- 从文件体积看，Parquet 更节省存储空间。
- 当数据规模更大、列更多、需要频繁按列读取，或者需要反复进行批量分析时，Parquet 的优势会更明显；因为它是列式存储，类型信息更完整，通常也有更好的压缩效果。而 CSV 更适合小规模数据、人工查看、跨软件交换和快速导入导出。

<!-- part2_end -->

### GitHub 仓库
https://github.com/tian1ruoyouyi1115/dshw-p01

### 如何运行
1. 安装依赖：`pip install -r requirements.txt`
2. 运行 `01_1.1_download.ipynb` 下载原始数据
3. 运行 `01_1.2-1.5_数据获取_可直接运行.ipynb` 获取数据
4. 运行 `02_clean.ipynb` 清洗并存储数据
5. 运行 `02_clean_第三部分数据清洗.ipynb` 清洗数据
6. 运行 `03_analysis.ipynb` 查看分析结果
7. 运行 `04_regression` 查看回归分析
8. 打开 `report.html` 阅读完整报告